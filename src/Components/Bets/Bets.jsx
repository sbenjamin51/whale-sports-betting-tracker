import Home from '../Home/Home'

const Bets = () => {
  return (
    <Home
      title="My Bets"
      subtitle="Track every wager, open ticket, and settled result in one place."
      statLabel="Open tickets"
      statValue="12"
    />
  )
}

export default Bets
